---
cbd:
  target: apps/web/core/components/workspace-notifications/sidebar/header/root.tsx
  kind: file
---

# Notification inbox back navigation

The workspace notification inbox header includes a back arrow before the inbox
title. It returns directly to the current workspace home at
`/${workspaceSlug}/`, matching the navigation pattern used by workspace
settings.
